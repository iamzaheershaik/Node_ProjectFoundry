<!DOCTYPE html>
<html lang="en">
<%- include('includes/head', {title: 'Edit Movie - ' + movie.title}) %>
<body>
    <%- include('includes/navbar') %>

    <div class="container">
        <div class="header-actions">
            <h2>✏️ Edit Movie Details</h2>
            <a href="/" class="back-link">← Back to Gallery</a>
        </div>

        <div class="edit-container">
            <div class="current-poster">
                <p>Current Poster:</p>
                <img src="/<%= movie.posterUrl %>" alt="Current Poster">
            </div>

            <form action="/update/<%= movie._id %>" method="POST" enctype="multipart/form-data" class="form">
                <div class="input-group">
                    <label>Movie Title</label>
                    <input type="text" name="title" value="<%= movie.title %>" required>
                </div>

                <div class="input-group">
                    <label>Director</label>
                    <input type="text" name="director" value="<%= movie.director %>" required>
                </div>

                <div class="row">
                    <div class="input-group">
                        <label>Release Year</label>
                        <input type="number" name="releaseYear" value="<%= movie.releaseYear %>" required>
                    </div>

                    <div class="input-group">
                        <label>Rating (1-10)</label>
                        <input type="number" name="rating" step="0.1" min="1" max="10" value="<%= movie.rating %>" required>
                    </div>
                </div>

                <div class="input-group">
                    <label>Genre</label>
                    <input type="text" name="genre" value="<%= movie.genre %>" required>
                </div>

                <div class="input-group">
                    <label>Update Poster (Leave empty to keep current)</label>
                    <input type="file" name="poster">
                </div>

                <button type="submit" class="btn-update">Save Changes</button>
            </form>
        </div>
    </div>

</body>
</html>