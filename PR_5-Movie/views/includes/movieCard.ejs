<div class="card">
    <img src="/<%= movie.posterUrl %>" alt="<%= movie.title %>">
    <div class="card-body">
        <h3><%= movie.title %></h3>
        <p><strong>Director:</strong> <%= movie.director %></p>
        <p><strong>Year:</strong> <%= movie.releaseYear %></p>
        <p><strong>Genre:</strong> <%= movie.genre %></p>
        
        <p class="rating">⭐ <strong><%= movie.rating %>/10</strong></p>
        
        <div class="actions">
            <a href="/edit/<%= movie._id %>">✏️</a>
            <a href="/delete/<%= movie._id %>">🗑️</a>
        </div>
    </div>
</div>
